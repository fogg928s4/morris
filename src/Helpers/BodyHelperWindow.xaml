<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Morris.Helpers.BodyHelperWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Morris"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Create Body"
    >


    <NavigationView x:Name="nvSample8" Grid.Row="1" Height="460"
                        PaneDisplayMode="Left"
                        IsTabStop="False">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Add Body Mass" Icon="AddTo" ToolTipService.ToolTip="Account" Tag="SamplePage2">
                <NavigationViewItem.MenuItems>
                    <NavigationViewItem Content="Add Star" Icon="FavoriteStarFill" ToolTipService.ToolTip="Mail" Tag="SamplePage3"/>
                    <NavigationViewItem Content="Add Planet" Icon="World" ToolTipService.ToolTip="Calendar" Tag="SamplePage4"/>
                </NavigationViewItem.MenuItems>
            </NavigationViewItem>
            <NavigationViewItem Content="Document options" Icon="Page2" ToolTipService.ToolTip="Document options" SelectsOnInvoked="False">
                <NavigationViewItem.MenuItems>
                    <NavigationViewItem Content="Create new" Icon="NewFolder" ToolTipService.ToolTip="Create new" Tag="SamplePage5"/>
                    <NavigationViewItem Content="Upload file" Icon="OpenLocal" ToolTipService.ToolTip="Upload file" Tag="SamplePage6"/>
                </NavigationViewItem.MenuItems>
            </NavigationViewItem>
        </NavigationView.MenuItems>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#CCD5F0">

                <!--Toggle btn with a custom glyph-->
                <ToggleButton x:Name="pencilBtn" IsChecked="True" Width="45" Height="45">
                    <FontIcon Foreground="Black" Glyph="&#xED63;"/>
                </ToggleButton>
                <ToggleButton x:Name="brushBtn" Width="45" Height="45">
                    <FontIcon Foreground="Black" Glyph="&#xE771;"/>
                </ToggleButton>

                <ToggleButton x:Name="eraserBtn" Width="45" Height="45">
                    <FontIcon Foreground="Black" Glyph="&#xE75C;"/>
                </ToggleButton>
            </StackPanel>
        </Grid>
    </NavigationView>



</Window>
