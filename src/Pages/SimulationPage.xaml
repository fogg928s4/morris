<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Morris.Pages.SimulationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Morris"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <!--SLIDER-->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="planetPosSliderLabel" Text="Change the planet position" Margin="10, 5, 10, 0"/>
                <Slider x:Name="planetPosSlider" ValueChanged="MovePlanetPos" Width="400"/>
            </StackPanel>
            <!--LABELS FOR SPEED-->
            <StackPanel Margin="10, 5,10,0"  Orientation="Horizontal" >
                <TextBlock x:Name="labelVelocity"  Margin="5,0,5,0"  Text="Satellite Speed = "/>
                <Border CornerRadius="4" Padding="4"  Margin="5,0,5,0"  Background="{StaticResource AccentTextFillColorTertiaryBrush}">
                    <TextBlock x:Name="velTxtBlock" Foreground="{StaticResource TextOnAccentFillColorPrimaryBrush}"  Margin="5,0,5,0"  Text="0.00 m/s"/>
                </Border>
            <!--LABELS FOR FORCE-->
                <TextBlock x:Name="labelForce" Text="Force Between = "  Margin="5,0,5,0" />
                <Border CornerRadius="4" Padding="5"  Margin="5,0,5,0"  Background="{StaticResource AccentTextFillColorTertiaryBrush}">
                    <TextBlock x:Name="forceTxtBlock" Foreground="{StaticResource TextOnAccentFillColorPrimaryBrush}" Text="0.00 kN"/>
                </Border>
            <!--LABELS FOR DISTANCE-->
                <TextBlock x:Name="labelDistance" Text="Distance Between = "  Margin="5,0,5,0" />
                <Border CornerRadius="4" Padding="5"  Margin="5,0,5,0"  Background="{StaticResource AccentTextFillColorTertiaryBrush}">
                    <TextBlock x:Name="distTxtBlock" Foreground="{StaticResource TextOnAccentFillColorPrimaryBrush}" Text="0.00 Mkm"/>
                </Border>
            </StackPanel>
        </StackPanel>
        
        <Canvas x:Name="canvas" Grid.Row="1" Background="#10134A" Height="Auto" Width="Auto" Loaded="Canvas_FillCanvas"/>

    </Grid>
</Page>
