<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Morris.Pages.SimulationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Morris"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Grid.Row="0" Grid.Column="0" x:Name="planetPosSliderLabel" Text="Change the planet position" Margin="10, 5, 10, 0"/>
            <Slider Grid.Row="0" Grid.Column="1" x:Name="planetPosSlider" ValueChanged="MovePlanetPos" Width="400"/>
        </StackPanel>
        <Canvas x:Name="canvas" Grid.Row="1" Background="#10134A" Height="Auto" Width="Auto" Loaded="Canvas_FillCanvas"/>
    </Grid>
</Page>
